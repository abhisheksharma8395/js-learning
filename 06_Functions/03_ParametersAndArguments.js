/*

Parameters and Arguments
A parameter is a variable in a function definition. An argument is the actual value that is passed to the function when it is called.

*/

function greet(name) {                              //Function Statement
    console.log("Hello, " + name + "!");  // Function body
}
greet("Alice"); // Calling the function with an argument